apiVersion: v1
kind: Service
metadata:
  name: app-recreate-service
  namespace: session10
spec:
  type: NodePort
  selector:
    app: app-recreate
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30040
